fastapi==0.115.6
starlette>=0.41,<1.0
# sse-starlette 3.x requires starlette>=1.0, which fastapi 0.115.6 cannot use
# (Router lost the on_startup kwarg in starlette 1.0). Pin <3 so the mcp[cli]
# transitive dep stays on starlette 0.41.x.
sse-starlette>=2.0,<3.0
uvicorn[standard]==0.34.0
pydantic>=2.13
pydantic-settings>=2.6
psutil==7.2.2
prometheus-client>=0.20
